{
  "name": "rhiz.om-root",
  "version": "1.0.0",
  "description": "Root project orchestrator for Rhiz.om",
  "scripts": {
    "install:all": "pnpm install",
    "dev": "concurrently --names \"WEB,API\" --prefix-colors \"bgBlue,bgGreen\" \"pnpm --filter web dev\" \"deno run --watch --allow-net --allow-env --allow-sys api/serve.ts\"",
    "lint": "deno lint"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}