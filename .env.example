# Google OAuth Client ID
# To get this, go to Google Cloud Console (console.cloud.google.com), create a project,
# navigate to "APIs & Services" > "Credentials", create an "OAuth client ID" for "Web application".
# Set "Authorized JavaScript origins" to your app's URL (e.g., http://localhost:3000).
# Set "Authorized redirect URIs" to your app's callback URL (e.g., http://localhost:3000/api/auth/callback/google).
# GOOGLE_CLIENT_ID=
# Google OAuth Client Secret
# This is generated along with the Client ID in the Google Cloud Console.
GOOGLE_CLIENT_SECRET=

# GitHub OAuth Client ID
# To get this, go to GitHub (github.com) > Settings > Developer settings > OAuth Apps.
# Register a new OAuth App. Set "Homepage URL" to your app's URL (e.g., http://localhost:3000).
# Set "Authorization callback URL" to your app's callback URL (e.g., http://localhost:3000/api/auth/callback/github).
GITHUB_CLIENT_ID=

# GitHub OAuth Client Secret
# This is generated after registering your OAuth App in GitHub Developer settings.
GITHUB_CLIENT_SECRET=

# Apple OAuth Client ID (Service ID)
# To get this, you need an Apple Developer Program account.
# Go to Apple Developer Portal > Certificates, Identifiers & Profiles > Identifiers.
# Create an App ID with "Sign In with Apple" capability.
# Then, create a Services ID (this will be your Client ID). Enable "Sign in with Apple" for this Service ID.
# Set "Return URLs" to your app's callback URL (e.g., https://your-domain.com/api/auth/callback/apple).
APPLE_ID=

# Apple OAuth Client Secret (JWT)
# This is a JSON Web Token (JWT) generated using a private key downloaded from Apple.
# Go to Apple Developer Portal > Certificates, Identifiers & Profiles > Keys.
# Create a new key, enable "Sign In with Apple", and download the .p8 key file.
# You will need your Team ID, Key ID, and the .p8 file to generate this JWT.
# NextAuth.js provides a script to help generate this secret: https://bal.so/apple-gen-secret
APPLE_SECRET=

# Facebook OAuth Client ID (App ID)
# To get this, go to Meta for Developers (developers.facebook.com), create an app.
# Navigate to "Settings" > "Basic" to find your App ID.
FACEBOOK_CLIENT_ID=

# Facebook OAuth Client Secret (App Secret)
# This is found alongside the App ID in Meta for Developers > Settings > Basic.
# You may need to click "Show" and re-enter your Facebook password to reveal it.
# Also, configure "Facebook Login" product, set "Valid OAuth Redirect URIs" to your app's callback URL
# (e.g., https://your-domain.com/api/auth/callback/facebook).
FACEBOOK_CLIENT_SECRET=

# NEXTAUTH_SECRET
# A random string used to encrypt session tokens and for other security purposes.
# It is crucial for production environments. You can generate one using `openssl rand -base64 32`
# or `npx auth secret` (for NextAuth.js v5+).
NEXTAUTH_SECRET=
NEXTAUTH_URL=